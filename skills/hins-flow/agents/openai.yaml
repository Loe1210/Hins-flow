interface:
  display_name: "Hins Flow"
  short_description: "Turns rough requests into verified, stable results"
  default_prompt: "Use $hins-flow to turn this rough request into a verified, stable result."
